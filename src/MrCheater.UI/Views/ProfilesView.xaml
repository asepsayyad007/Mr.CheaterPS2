<UserControl x:Class="MrCheater.UI.Views.ProfilesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:MrCheater.UI.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:ProfilesViewModel}"
             d:DesignHeight="750" d:DesignWidth="900">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Top Header & Import/Export Actions -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Game Profiles Manager" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" />
                <TextBlock Text="Create, configure, switch, import and export game profiles" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2,0,0" />
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Import Profile (.json)" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0" Command="{Binding ImportProfileCommand}" />
                <Button Content="Export Profile" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0" Command="{Binding ExportProfileCommand}" />
                <Button Content="+ Create Profile" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding CreateNewProfileCommand}" />
            </StackPanel>
        </Grid>

        <!-- Main Profiles Layout: Left List, Right Profile Details -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Profiles List -->
            <Border Grid.Column="0" Style="{StaticResource ModernCardStyle}" Margin="0,0,16,0" Padding="10">
                <ListBox ItemsSource="{Binding Profiles}"
                         SelectedItem="{Binding SelectedProfile}"
                         Background="Transparent"
                         BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="10,8" Margin="0,0,0,4" CornerRadius="6" Background="{StaticResource SurfaceHoverBrush}">
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" Foreground="{StaticResource TextPrimaryBrush}" TextTrimming="CharacterEllipsis" />
                                    <DockPanel Margin="0,4,0,0">
                                        <TextBlock Text="{Binding EmulatorName}" FontSize="12" Foreground="{StaticResource AccentPrimaryBrush}" />
                                        <TextBlock Text="{Binding Cheats.Count, StringFormat='{}{0} cheats'}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Right" />
                                    </DockPanel>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <!-- Right Details Editor -->
            <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
                <Border Style="{StaticResource ModernCardStyle}">
                    <StackPanel>
                        <DockPanel Margin="0,0,0,16">
                            <TextBlock Text="PROFILE CONFIGURATION" FontSize="12" FontWeight="Bold" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center" />
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Content="Set As Active Profile" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,8,0" Command="{Binding SetActiveCommand}" CommandParameter="{Binding SelectedProfile}" />
                                <Button Content="Duplicate" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0" Command="{Binding DuplicateProfileCommand}" CommandParameter="{Binding SelectedProfile}" />
                                <Button Content="Delete" Style="{StaticResource DangerButtonStyle}" Command="{Binding DeleteProfileCommand}" CommandParameter="{Binding SelectedProfile}" />
                            </StackPanel>
                        </DockPanel>

                        <!-- Game Name & ID -->
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                <TextBlock Text="Game Name:" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4" />
                                <TextBox Text="{Binding SelectedProfile.Name, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>

                            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                <TextBlock Text="Game Code / ID:" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4" />
                                <TextBox Text="{Binding SelectedProfile.GameCode, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                        </Grid>

                        <!-- Target Process & Emulator -->
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                <TextBlock Text="Emulator / Platform:" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4" />
                                <TextBox Text="{Binding SelectedProfile.EmulatorName, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>

                            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                <TextBlock Text="Target Process Executable:" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4" />
                                <TextBox Text="{Binding SelectedProfile.TargetProcess, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                        </Grid>

                        <!-- Window Title Pattern -->
                        <TextBlock Text="Window Title Pattern (Optional):" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4" />
                        <TextBox Text="{Binding SelectedProfile.WindowTitlePattern, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

                        <!-- Timing Defaults -->
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                <TextBlock Text="Default Step Delay (ms):" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4" />
                                <TextBox Text="{Binding SelectedProfile.DefaultStepDelayMs, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>

                            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                <TextBlock Text="Default Hold Duration (ms):" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4" />
                                <TextBox Text="{Binding SelectedProfile.DefaultHoldDurationMs, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                        </Grid>

                        <!-- Notes -->
                        <TextBlock Text="Profile Notes:" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4" />
                        <TextBox Text="{Binding SelectedProfile.Notes, UpdateSourceTrigger=PropertyChanged}" Height="70" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,16" />

                        <!-- Save Button -->
                        <Button Content="Save Profile Changes" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Right" Padding="18,8" Command="{Binding SaveSelectedProfileCommand}" />

                    </StackPanel>
                </Border>
            </ScrollViewer>

        </Grid>
    </Grid>
</UserControl>
